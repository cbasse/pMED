<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="30dp"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.pmed.mindfulnessmeditation.Audio"
    android:background="#3a4f9c">

    <TextView
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Listen carefully to the audio recording"
        android:id="@+id/text_description"
        android:layout_marginTop="60dp"
        android:layout_centerHorizontal="true"
        android:gravity="center_horizontal"
        android:textSize="40dp"
        android:textColor="#ffffffff"/>



    <ImageView
        android:id="@+id/image_audio"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/icon_audio"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:onClick="onClickButton"
        />

    <TextView
        android:id="@+id/connect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap to Play"
        android:textColor="#ffffff"
        android:textSize="25dp"
        android:layout_above="@id/image_audio"
        android:layout_marginBottom="20dp"
        android:layout_centerHorizontal="true"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_audio"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        android:text="Next"
        android:id="@+id/button_audio_next"
        android:onClick="onClickButton"
        android:layout_marginTop="50dp"
        android:background="#ffffff" />

</RelativeLayout>